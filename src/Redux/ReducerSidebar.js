

let initialState = {
    names: [
        {id: 0, name: 'Rustam'},
        {id: 0, name: 'Olga'},
        {id: 0, name: 'Adele'}
    ]
};

const reducerSidebar = (state=initialState, action) => {


    return state;
}

export default reducerSidebar;